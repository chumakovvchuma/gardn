type PlantStage implements BaseDbModel {
  id: Int!
  dateCreated: Timestamp!
  
  name: String!
  description: String

  defaultEstimateLength: Int
  defaultEnvironment: Environment
}

type Query {
  plantStages: [PlantStage]
  plantStage(id: Int!): PlantStage
}